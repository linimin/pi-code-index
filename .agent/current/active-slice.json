{
  "schema_version": 1,
  "mission_anchor": "Fully implement `pi-code-index` across all explicitly documented project phases available in the repository, starting from the normative Phase 1 MVP and continuing through any additional concrete phase specs, while preserving the documented architecture of a thin pi extension backed by a local daemon.",
  "task_type": "completion-workflow",
  "evaluation_profile": "completion-rubric-v1",
  "status": "committed",
  "slice_id": "p1-index-storage-and-analysis",
  "goal": "Implement the Phase 1 indexing core: git-aware discovery and safety filtering, transactional SQLite baseline plus overlay lifecycle, TS/JS structural fact extraction, coarse non-primary fallback summaries, and truthful `/index disable` plus `/index reindex` state transitions.",
  "contract_ids": [
    "P1-INDEX-STORAGE-PIPELINE"
  ],
  "acceptance_criteria": [
    "Git-aware file discovery is bounded to the canonical repo root and finds eligible files via tracked files, modified tracked files, and untracked non-ignored files only, while enforcing the Phase 1 hard safety boundaries, default excludes, sensitive-file denylist, binary detection, and 2 MiB size cap.",
    "The daemon maintains one immutable baseline DB per commit and one mutable overlay DB per worktree, commits query-visible writes transactionally, and answers status or doctor with truthful coverage, pending-file, and storage metadata.",
    "TS/JS files receive structural symbol, import, export, reference, and file-summary extraction with `analysisQuality: structural`, while non-primary languages receive coarse file-summary fallback with `analysisQuality: basic` only.",
    "`/index disable` stops repo background activity without purging cache by default, `/index reindex` performs a soft rebuild, and deterministic tests exercise `initializing`, `indexing`, `ready`, `stale`, `error`, and `disabled` transitions."
  ],
  "priority": 2,
  "why_now": "A real control plane and repo registration path are prerequisite for truthful indexing, storage, and lifecycle semantics.",
  "blocked_on": [],
  "locked_notes": [
    "One immutable baseline DB per commit plus one mutable overlay DB per worktree is mandatory in Phase 1.",
    "TS/JS are the only primary structural analyzers; non-primary languages must stay coarse/fallback-only in MVP."
  ],
  "must_fix_findings": [],
  "implementation_surfaces": [
    "package.json",
    "src/daemon/repo-runtime.ts",
    "src/daemon/server.ts",
    "src/daemon/sqlite-store.ts",
    "src/daemon/tsjs-analyzer.ts",
    "src/shared/protocol.ts"
  ],
  "verification_commands": [
    "npm run check",
    "npm test"
  ],
  "basis_commit": "329dab218489e7abf9ad579f006eb671d032a6b3",
  "remaining_contract_ids_before": [
    "P1-INDEX-STORAGE-PIPELINE",
    "P1-TOOLS-DIAGNOSTICS"
  ],
  "release_blocker_count_before": 2,
  "high_value_gap_count_before": 0
}
