{
  "name": "@linimin/pi-code-index",
  "version": "0.1.0",
  "description": "Local-first code indexing for pi with a thin extension and background daemon.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.6.0"
  },
  "keywords": [
    "pi-package",
    "pi",
    "extension",
    "indexing",
    "code-intelligence"
  ],
  "files": [
    "extensions",
    "src",
    "docs",
    "AGENTS.md",
    "README.md",
    "tsconfig.json"
  ],
  "scripts": {
    "check": "node --experimental-strip-types ./tests/check.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test ./tests/*.test.ts",
    "dev:daemon": "node --experimental-strip-types ./src/daemon/main.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typebox": "*",
    "typescript": "^5.5.0"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
